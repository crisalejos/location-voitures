{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}

<div class="row mt-5">
<div class="col-md-4 offset-1 d-none d-md-block">
    <img src="{{ asset('build/images/register.jpg') }}" class="d-block w-100" alt="Location de la voiture">
</div>
<div class="col-md-6 ">


<h1 class="h3 mb-3  font-weight-normal ">Inscription</h1>

    {{ form_start(registrationForm) }}
    <div class="row mt-3">
        <div class="col-md-6 ">{{ form_row(registrationForm.firstname) }}</div>
        <div class="col-md-6 ">{{ form_row(registrationForm.lastname) }}</div>    
    </div> 
    <div class="row mt-3">
        <div class="col-md-6 ">{{ form_row(registrationForm.phone) }}</div>
        <div class="col-md-6 ">{{ form_row(registrationForm.card) }}</div>    
    </div> 
    <div class="row mt-3">
        <div class="col-md-12 form-group">{{ form_row(registrationForm.birthdate) }}</div>          
    </div>
    <div class="row mt-3">
        <div class="col-md-12 ">              
            {{ form_row(registrationForm.email) }}
           
        </div> 
    </div>
    <div class="row mt-3">
        <div class="col-md-12 ">              
            
            {{ form_row(registrationForm.plainPassword) }}
        </div> 
    </div>
    <div class="row m-3">
        <div class="col-md-12 form-check">                          
            {{ form_row(registrationForm.agreeTerms) }}
        </div> 
    </div>
    <div class="row ">
        <div class="col-md-12 ">
        <button type="submit" class="mt-3 btn btn-primary">Céer un compte</button>
    </div> 
    </div>
    {{ form_end(registrationForm) }}

</div>
</div>

    
{% endblock %}
