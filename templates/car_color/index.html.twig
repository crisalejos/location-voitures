{% extends 'base.html.twig' %}

{% block title %}CarColor index{% endblock %}

{% block body %}
    <h1>CarColor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Color</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for car_color in car_colors %}
            <tr>
                <td>{{ car_color.id }}</td>
                <td>{{ car_color.color }}</td>
                <td>
                    <a href="{{ path('car_color_show', {'id': car_color.id}) }}">show</a>
                    <a href="{{ path('car_color_edit', {'id': car_color.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('car_color_new') }}">Create new</a>
{% endblock %}
