{% extends 'base.html.twig' %}

{% block title %}CarColor{% endblock %}

{% block body %}
    <h1>CarColor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_color.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ car_color.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('car_color_index') }}">back to list</a>

    <a href="{{ path('car_color_edit', {'id': car_color.id}) }}">edit</a>

    {{ include('car_color/_delete_form.html.twig') }}
{% endblock %}
