<form method="post" action="{{ path('car_color_delete', {'id': car_color.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_color.id) }}">
    <button class="btn">Delete</button>
</form>
