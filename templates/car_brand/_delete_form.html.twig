<form method="post" action="{{ path('car_brand_delete', {'id': car_brand.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_brand.id) }}">
    <button class="btn">Delete</button>
</form>
