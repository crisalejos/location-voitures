{% extends 'base.html.twig' %}

{% block title %}CarBrand{% endblock %}

{% block body %}
    <h1>CarBrand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car_brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('car_brand_index') }}">back to list</a>

    <a href="{{ path('car_brand_edit', {'id': car_brand.id}) }}">edit</a>

    {{ include('car_brand/_delete_form.html.twig') }}
{% endblock %}
